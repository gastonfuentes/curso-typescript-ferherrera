"use strict";
(() => {
    const fullName = (firstName, lastName) => {
        return `${firstName} ${lastName}`;
    };
    const name = fullName('Tony', 'Fuentes');
    console.log(name);
})();
//# sourceMappingURL=args-required.js.map