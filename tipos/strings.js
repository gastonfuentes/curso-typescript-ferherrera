"use strict";
(() => {
    var _a;
    const batman = 'Batman';
    const linternaVerde = "linterna verde";
    const volcan = `heroe: volvan negro`;
    console.log(batman.toUpperCase());
    console.log(`el heroe mas copado es ${volcan}`);
    console.log((_a = batman[10]) === null || _a === void 0 ? void 0 : _a.toUpperCase);
})();
